# ----------------------------------------------------------------
# Redirect old "Ubuntu Server" blog URLs → new "Linux" paths
# ----------------------------------------------------------------

# Redirect encoded space (%20)
if ($request_uri ~* "^/pages/blog/local_html/Ubuntu%20Server/(.*)$") {
    return 301 /pages/blog/local_html/Linux/$1;
}

# Redirect literal space version
if ($request_uri ~* "^/pages/blog/local_html/Ubuntu Server/(.*)$") {
    return 301 /pages/blog/local_html/Linux/$1;
}